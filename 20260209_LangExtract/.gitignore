# Cloned third-party repo (google/langextract) — has its own .git/
langextract/

# Claude Code local settings (contains local paths)
.claude/

# Environment variables (contains API tokens/secrets)
.env
.env.*

# Python virtual environments and build artifacts
.venv/
venv/
*.egg-info/
__pycache__/
*.pyc

# Re-allow file types blocked by parent .gitignore
!*.csv
!*.txt
*.md
*.lock

# Data files — tracked via HF dataset repo in data/
data/

# Large binary data (PDFs, archives)
semiconductor_processing_dataset/raw_documents_R2/papers/
semiconductor_processing_dataset/*.zip
semiconductor_processing_dataset/raw_documents_R2/_tmp_browser_downloads/*