[project]
name = "usgs-china-map-viewer"
version = "0.1.0"
description = "Minimal interactive frontend for exploring USGS China mineral and infrastructure layers."
requires-python = ">=3.12"
dependencies = [
  "geopandas>=1.1.3",
  "pyogrio>=0.12.1",
  "shapely>=2.1.2",
]

